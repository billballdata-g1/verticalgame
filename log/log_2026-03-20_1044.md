# Step 1: Phaser 3 项目初始化 + 玩家移动

**时间**: 2026-03-20 10:44 GMT+8
**版本**: v1.0.0

---

## 📝 工作日志

### ✅ 完成的工作

#### **1. 清理旧项目并初始化**
```bash
rm -rf * .* 2>/dev/null    # 清空目录（因为之前用的是原生 Canvas）
npm init -y                # npm 初始化
```

---

#### **2. 安装依赖包**

| 包名 | 版本 | 用途 |
|------|------|------|
| `phaser` | ^3.70.0 | Phaser 游戏引擎核心 |
| `vite` | ^5.0.0 | 开发服务器 + 打包工具（devDependencies）|

```bash
npm install phaser@3 --save      # 安装 Phaser 作为依赖
npm install --save-dev vite      # 安装 Vite 作为开发依赖
```

---

#### **3. 创建文件**

| 文件 | 说明 |
|------|------|
| `/game/package.json` | npm 配置文件，包含脚本命令 |
| `/game/index.html` | HTML 入口页面 |
| `/game/src/main.js` | Phaser 游戏主逻辑 |
| `/game/vite.config.js` | Vite 配置（端口 3000，自动打开）|

---

## 📁 项目结构

```
/home/billv/workspace/game/
├── node_modules/          ← 依赖包目录
│   ├── phaser            ← Phaser 引擎 (28K+ stars)
│   └── vite              ← Vite 开发服务器
├── src/
│   └── main.js           ← 游戏主逻辑（Phaser Scene）
├── log/
│   └── log_2026-03-20_1044.md   ← 📄 本日志文件
├── index.html            ← HTML 入口
├── package.json          ← npm 配置
└── vite.config.js        ← Vite 配置
```

---

## 🔧 修改的文件详情

### **package.json** (新建)
```json
{
  "name": "platformer-game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",        ← npm run dev → 启动开发服务器
    "build": "vite build",← npm run build → 打包生产版本
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.70.0"   ← Phaser 引擎
  },
  "devDependencies": {
    "vite": "^5.0.0"      ← Vite
  }
}
```

---

### **src/main.js** (新建) - 核心代码
```javascript
import Phaser from 'phaser';

const config = {
    type: Phaser.AUTO,
    width: 800,
    height: 600,
    physics: { default: 'arcade', arcade: { gravity: { y: 1200 } } },
    scene: { preload, create, update }
};

const game = new Phaser.Game(config);
```

---

## 🎮 当前功能

| 功能 | 状态 |
|------|------|
| ✅ Phaser 3.70 初始化 | 完成 |
| ✅ Arcade Physics 物理引擎 | 启用 |
| ✅ 玩家角色（红色方块） | ← → 移动，↑/空格 跳跃 |
| ✅ 地面碰撞检测 | 不会掉出屏幕 |
| ✅ 调试信息显示 | 左上角显示位置/速度 |

---

## 🚀 如何运行

```bash
# 进入项目目录
cd /home/billv/workspace/game

# 启动开发服务器（自动打开浏览器）
npm run dev
```

访问：http://localhost:3000

---

## 📦 npm 包信息

```bash
added 13 packages, and audited 13 packages in ~50s

Packages:
- phaser (28K+ GitHub stars) ← 业界标准 HTML5 游戏引擎
- vite (开发工具，超快的 HMR)
```

---

## 🎯 Step 2 预告

下一步可选：
1. **平台系统** — 在空中搭建可站立的平台
2. **敌人系统** — 简单巡逻的方块，碰到会死
3. **踩扁机制** — 从上方落下时消灭敌人（马里奥经典！）

---

*日志格式：log_YYYY-MM-DD_HHMM.md*
