# Log - Step 3d-1: ItemManager 框架搭建

**时间**: 2026-03-22 21:40 GMT+8  
**状态**: ✅ COMPLETED

---

## 📋 **任务目标**

创建物品系统的核心框架，包含：
- `types.js` — 类型定义和常量
- `ItemManager.js` — 装备系统管理器
- `index.js` — 统一导出口

参考 `src/enemies/` 的模块化架构设计。

---

## 🎯 **已创建文件**

### **1️⃣ src/items/types.js** (3.4KB)

包含三大核心定义：

#### **A. EQUIPMENT_SLOTS（装备槽位）**
```javascript
export const EQUIPMENT_SLOTS = {
    HEAD: 'head',     // 👑 头部
    CHEST: 'chest',   // 🛡️ 胸部
    HANDS: 'hands',   // ✋ 手部
    FEET: 'feet',     // 👟 脚部
};
```

#### **B. ITEM_TYPES（物品类型）**
```javascript
export const ITEM_TYPES = {
    EQUIPMENT: 'equipment',   // 🎒 装备类
    CONSUMABLE: 'consumable', // 💊 消耗品
    COLLECTIBLE: 'collectible',// 💰 收集品
};
```

#### **C. EFFECT_TYPES（效果类型）**
```javascript
export const EFFECT_TYPES = {
    STAT_MODIFY: 'stat_modify',      // ⚡ 属性修改
    ABILITY_UNLOCK: 'ability_unlock',// 🔓 能力解锁
    BEHAVIOR_CHANGE: 'behavior_change',// 🔄 行为改变
};
```

#### **D. itemConfig 接口定义（8 个核心维度）**
```javascript
/**
 * @typedef {Object} ItemConfig
 * 
 * @property {string} id - 唯一标识
 * @property {Object} spawnPosition - 初始化位置
 * @property {Object} collectibleBy - 谁可以收集
 * @property {Object} visualization - 可视化设置（persist, attachToBodyPart）
 * @property {Object|null} equipmentSlot - 装备槽位配置
 * @property {Object[]} effects - 物品功能效果数组
 * @property {Object|null} upgradeable - 升级机制配置
 * @property {Object|null} duration - 持久化时间配置
 * @property {Object|null} destructible - 可破坏性配置
 */
```

---

### **2️⃣ src/items/ItemManager.js** (4.9KB)

核心管理器类，包含以下方法：

| 方法 | 说明 |
|------|------|
| `constructor(scene)` | 初始化装备槽位 Map、效果列表 |
| `collect(itemConfig)` | 收集物品，自动装备 + 应用效果 |
| `equip(slotName, itemConfig)` | 装备/更换物品到指定槽位 |
| `unequip(slotName)` | 卸下物品并移除效果 |
| `applyEffects(itemId, effects)` | 应用物品效果（TODO: 具体实现） |
| `removeEffects(itemId)` | 移除物品效果（TODO: 具体实现） |
| `upgrade(itemId)` | 升级物品（TODO: 具体实现） |
| `attachVisual(itemSprite, player, bodyPart, offset)` | 附着视觉元素到玩家身体部位 |
| `update()` | 每帧更新，处理跟随移动逻辑 |

**关键数据结构：**
```javascript
this.equippedItems = new Map();     // slotName → itemConfig
this.activeEffects = [];            // 激活的效果列表
this.attachedVisuals = new Map();   // itemSprite → {player, bodyPart}
```

---

### **3️⃣ src/items/index.js** (0.6KB)

统一导出口：
```javascript
export {
    EQUIPMENT_SLOTS,
    ITEM_TYPES,
    EFFECT_TYPES,
} from './types.js';

export { ItemManager, createItemManager } from './ItemManager.js';
```

---

## 📁 **目录结构**

```bash
src/items/
├── types.js          ✅ 3.4KB — 类型定义和常量
├── index.js          ✅ 0.6KB — 统一导出口  
└── ItemManager.js    ✅ 4.9KB — 核心管理器
```

---

## 🎯 **下一步：Step 3d-2**

**任务**: 把 Speed Shoes 从 `main.js` 抽离到模块文件中。

**计划文件**: `src/items/speedShoesItem.js`

**需要做的事情：**
1. 创建符合 `itemConfig` 规范的配置对象
2. 实现 `create()` — 生成鞋子 Sprite
3. 实现 `collect()` — 收集逻辑（二段跳解锁 + 附着到脚底）
4. 在 `main.js` 中使用模块化 API 替换内联代码

---

## 📝 **TODO: ItemManager 待实现功能**

- [ ] `applyEffects()` — 根据效果类型修改玩家属性/解锁能力
- [ ] `removeEffects()` — 恢复原始状态（卸下装备时）
- [ ] `upgrade()` — 物品升级逻辑 + 等级提升效果
- [ ] Duration 系统 — 定时过期物品的处理
- [ ] Destructible 系统 — 可破坏物品的 HP 管理

---

*日志版本：v1.0*
