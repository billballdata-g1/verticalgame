# Step 1: Phaser 3 项目初始化 + Git 版本管理

**时间**: 2026-03-20 11:13 GMT+8
**版本**: v1.0.0

---

## 📝 工作日志

### ✅ 完成的工作

#### **Step 1a: Phaser 项目初始化**
```bash
mkdir -p /home/billv/workspace/game && cd game
npm init -y
npm install phaser@3 --save      # Phaser 游戏引擎
npm install --save-dev vite       # Vite 开发服务器
```

---

#### **Step 1b: Git 版本管理设置**
```bash
git init                          # 初始化 Git 仓库
echo "node_modules/" > .gitignore # 忽略依赖包目录
git branch -m main                # 主分支改名为 main
```

---

#### **Step 1c: GitHub 远程仓库**
```bash
# 访问 https://github.com/new 创建空仓库：verticalgame
# Token 权限问题后手动解决，最终成功 Push！

git remote add origin https://github.com/billballdata-g1/verticalgame.git
git push -u origin main           # ✅ SUCCESS!
```

---

## 📁 项目结构

```
/home/billv/workspace/game/
├── node_modules/          ← 依赖包目录（已安装）
│   ├── phaser            ← Phaser 引擎 (28K+ stars)
│   └── vite              ← Vite 开发服务器
├── src/
│   └── main.js           ← 游戏主逻辑（Phaser Scene）
├── log/
│   ├── log_2026-03-20_1044.md   ← Step 1a 日志
│   └── log_2026-03-20_1113.md   ← 📄 本日志文件（Step 1b + 版本管理）
├── index.html            ← HTML 入口
├── package.json          ← npm 配置
├── vite.config.js        ← Vite 配置
└── .gitignore            ← Git 忽略规则
```

---

## 🎮 当前功能（Step 1）

| 功能 | 状态 |
|------|------|
| ✅ Phaser 3.70 初始化 | 完成 |
| ✅ Arcade Physics 物理引擎 | 启用 |
| ✅ 玩家角色（红色方块） | ← → 移动，↑/空格 跳跃 |
| ✅ 地面碰撞检测 | 不会掉出屏幕 |
| ✅ 调试信息显示 | 左上角显示位置/速度 |

---

## 🚀 如何运行

```bash
# 进入项目目录
cd /home/billv/workspace/game

# 启动开发服务器（自动打开浏览器）
npm run dev
```

访问：http://localhost:3000

---

## 🔄 版本管理命令速查

### **Step N 完成后保存：**
```bash
git add . && git commit -m "Step N: xxx"
```

### **如果出错，回退到上一个版本：**
```bash
git revert HEAD        # 撤销最近一次提交
# 或者：
git log --oneline      # 查看历史
```

---

## 🎯 Step 2 预告

下一步可选：
1. **📦 平台系统** — 在空中加可站立的平台
2. **👾 敌人系统** — 简单巡逻的方块，碰到会死  
3. **⬇️ 踩扁机制** — 从上方落下时消灭敌人（马里奥经典！）

---

*日志格式：log_YYYY-MM-DD_HHMM.md*
